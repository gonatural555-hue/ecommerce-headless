/**
 * PDP “star” / hero ancho: fotos horizontales (p. ej. gafas) sin recorte agresivo.
 * Desktop: más columna de galería + carrusel tipo hero; móvil: mismo id usa contenedor full-bleed (ver ProductDetailClient).
 */
export const PDP_HERO_WIDE_PRODUCT_IDS = new Set<string>(["gn-cycling-010"]);
