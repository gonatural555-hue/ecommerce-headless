"use client";

// DEBUG VERSION - NO CONDITIONS, FORCE VISIBLE
export default function RegistrationCTADebug() {
  return (
    <div
      style={{
        position: "fixed",
        bottom: "16px",
        left: "16px",
        zIndex: 9999,
        backgroundColor: "#ff0000",
        color: "#ffffff",
        padding: "20px",
        fontSize: "24px",
        fontWeight: "bold",
        borderRadius: "8px",
        boxShadow: "0 4px 12px rgba(0,0,0,0.5)",
      }}
    >
      CTA REGISTER DEBUG
    </div>
  );
}

